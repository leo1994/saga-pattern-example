{
  "name": "@marketplace/server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc -b",
    "watch": "tsc -b -w",
    "clean": "shx rm -rf dist"
  },
  "dependencies": {
    "@marketplace/store": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^6.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.15"
  }
}
